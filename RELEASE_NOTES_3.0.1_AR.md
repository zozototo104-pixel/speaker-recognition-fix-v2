# ملاحظات إصدار الخبير الذكي المؤسسي 3.0.1

إصدار جاهزية نشر يحافظ على إمكانات 3.0 ويضيف طبقة تشغيل إنتاجية آمنة.

## ما الجديد؟

- حاوية إنتاج متعددة المراحل تعمل على Node.js 22 وبمستخدم غير مميز.
- نقطة حياة `/api/health` ونقطة جاهزية `/api/ready` تتحقق من PostgreSQL ونموذج المتحدث العصبي.
- دعم `DATABASE_URL` وTLS ومجموعة إعدادات PostgreSQL التقليدية.
- ترحيل تأسيسي لقاعدة البيانات وسجل `schema_migrations` ببصمة SHA-256 وقفل يمنع تنفيذ الهجرات بالتوازي.
- دعم Firebase Admin عبر هوية Google Cloud أو سر JSON محمي لدى مزود استضافة آخر.
- إغلاق آمن لاتصالات HTTP وPostgreSQL عند تحديث النسخة أو إيقافها.
- رؤوس أمان أساسية ومنع تخزين استجابات API في ذاكرة التخزين المؤقت.
- دليل نشر عربي يوضح الأسرار والترحيلات وفحوص الصحة ومتطلبات WebSocket.

## التحقق

- TypeScript: ناجح.
- اختبارات الوحدة: 14 ناجحة، 0 فاشلة.
- نموذج المتحدث: وضع `NEURAL`، وبُعد 512، وبصمة SHA-256 مطابقة.
- بناء React/Vite وخادم Node وأداة الترحيل: ناجح.
- تشغيل نسخة الإنتاج وفحص `/api/health`: ناجح.
- عند غياب قاعدة البيانات يعيد `/api/ready` الحالة 503 كما هو مطلوب.
