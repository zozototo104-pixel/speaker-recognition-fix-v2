# تقرير الفحص الشامل والإصلاح — الخبير الذكي

تاريخ الفحص: 18 أغسطس 2026  
الإصدار الأساسي: 3.0.2  
نطاق الفحص: 68 ملف كود، نحو 20,884 سطر، و46 مسار HTTP، إضافة إلى الإعدادات والترحيلات والاختبارات.

## الخلاصة

تمت مراجعة بنية المشروع كاملة: الخادم، الواجهة، الخبير متعدد التخصصات، RAG وقاعدة المعرفة، رفع الملفات، الحوار الصوتي، بصمة المتحدث، الاجتماعات، القرارات والمهام والمخاطر والمخالفات، المصادقة، التكاملات، الترحيلات، CI، Docker، وملفات البيئة.

أهم عيب وظيفي مؤكد كان في مسار PDF: دالة فحص جودة النص كانت تعتبر أي نص أطول من 8 أحرف صالحاً، ولذلك كانت نصوص PDF ذات خرائط الخطوط العربية التالفة تمر إلى قاعدة المعرفة وتظهر كرموز مشوهة، ثم يمكن للخبير استخدامها كسياق. تم إصلاح هذا جذرياً.

## الإصلاحات المنفذة

### 1) PDF وقاعدة المعرفة — إصلاح جذري

- إعادة بناء `DocumentTextQuality.ts` ليقيس:
  - الأحرف العربية واللاتينية والأرقام.
  - محارف الاستبدال `�`.
  - محارف التحكم.
  - نطاقات Private Use الناتجة عن خرائط خطوط تالفة.
  - مؤشرات mojibake.
  - الانتقالات المباشرة غير الطبيعية بين العربية واللاتينية.
  - الرموز غير المتوقعة.
- عند اكتشاف نص PDF غير موثوق، ينتقل الخادم إلى مسار OCR الموثق الموجود أصلاً بدلاً من حفظ النص التالف.
- بعد OCR يعاد فحص جودة النص قبل قبوله.
- إضافة اختبار وحدة يحاكي نص PDF عربي مكسور ويضمن رفضه.
- إضافة فحص جودة عند استرجاع مستندات RAG: أي مستند قديم مشوه يعزل تلقائياً ولا يصل إلى الخبير.
- واجهة قاعدة المعرفة أصبحت تعرض تحذيراً واضحاً إذا كان مستند قديم غير موثوق، وتوضح أنه معزول عن الخبير ويجب حذفه وإعادة رفع الأصل.
- رسالة الرفع توضح عندما تم استخدام `VERIFIED_OCR`.

مهم: الملفات القديمة التي خُزنت بنص مشوه لا يمكن إعادة OCR لها من قاعدة البيانات لأن النظام لا يحتفظ بملف PDF الأصلي نفسه؛ يجب حذف المرجع القديم وإعادة رفع ملف PDF بعد نشر هذه النسخة.

### 2) الخبير الذكي

تمت مراجعة:
- كتالوج الخبراء وتعدد التخصصات.
- اختيار قائد اللوحة وحد الأربعة خبراء.
- تعليمات عدم اختلاق اللوائح أو الوقائع.
- إنشاء المخرجات المهنية كمسودات غير معتمدة.
- الفصل بين القرار المعتمد والتوصية.
- المخالفات: لا تتحول إلى مؤكدة دون مرجع ونص ودليل.
- RAG: منع تسرب مستندات مؤسسة أخرى، والآن أيضاً منع المستندات المشوهة.
- استخراج القرارات والمهام والمخاطر والملاحظات من الاجتماعات دون عناصر افتراضية.

### 3) الحوار الصوتي وهوية المتحدث

تمت مراجعة:
- بدء جلسة الصوت والمهل الزمنية.
- إرسال النص الحي إلى Gemini Live.
- إعادة أخذ عينات الصوت إلى 16kHz.
- سجل المتحدث ومنع فرض هوية على صوت مجهول.
- رفض التطابق الملتبس.
- فصل إصدارات نماذج البصمة الصوتية.
- حفظ اسم المتحدث ودرجة الثقة في الرسائل.

القيد القائم: ملف نموذج ONNX المرفق داخل `models` حجمه صغير جداً وغير صالح كنموذج عصبي فعلي، لذلك الاعتماد الإنتاجي الكامل على التعرف العصبي للمتحدث يتطلب تنزيل النموذج الصحيح عبر آلية المشروع والتحقق منه. النظام لديه وضع fallback آمن ولا ينبغي اعتبار fallback إثبات هوية قطعي.

### 4) المصادقة وعزل المؤسسات

تم فحص 46 مساراً.
المسارات غير المحمية مباشرة بالمصادقة هي فقط المسارات المقصودة لذلك: health/ready، الدخول المباشر المقيد بإعدادات الإنتاج، webhook الخاص بالاتصال الخارجي، ومسار SPA. بقية مسارات البيانات الحساسة تستخدم `requireAuth`.

### 5) سلامة المشروع

- لا توجد ملفات مصدر فارغة.
- جميع الاستيرادات النسبية تشير إلى ملفات موجودة.
- لا يوجد `eval` أو `new Function`.
- لا يوجد `dangerouslySetInnerHTML`.
- لا توجد مفاتيح خاصة/توكنات GitHub/Stripe مضمنة في المصدر. مفتاح Firebase الموجود في إعداد العميل هو إعداد عميل Firebase وليس مفتاح خدمة خاصاً.
- رفع الملفات محدود بحجم 25MB وملف واحد.
- امتدادات قاعدة المعرفة محصورة في PDF/DOCX/XLSX/CSV/TXT/MD.

## الاختبارات المنفذة داخل بيئة الفحص

نجحت الاختبارات التالية:
- `document_text_quality.unit.test.ts`: 4/4
- `direct_auth.unit.test.ts`: 1/1
- `deliverable_safety.unit.test.ts`: 2/2
- `speaker_registry.unit.test.ts`: 6/6

المجموع المنفذ هنا: 13 اختباراً ناجحاً.

تعذر تشغيل دورة `npm ci -> lint -> build` كاملة في بيئة الفحص المحلية لأن نسخة `node_modules` المرفقة كانت غير مكتملة ومحاولة إعادة تثبيت الحزم اصطدمت بصلاحيات نظام الملفات في الحاوية. لذلك يجب أن يظل GitHub Actions هو بوابة الاعتماد النهائية بعد رفع هذه النسخة. النسخة السابقة كانت قد اجتازت CI وRender قبل هذه التعديلات.

## اختبار القبول المطلوب بعد النشر

1. ارفع النسخة إلى GitHub وانتظر CI أخضر.
2. انتظر Render حتى يصبح Deploy live.
3. احذف PDF القديم المشوه من قاعدة المعرفة.
4. أعد رفع نفس PDF الأصلي.
5. يجب أن تظهر رسالة نجاح تشير إلى OCR الموثق إذا اكتشف النظام خريطة خط تالفة.
6. افتح المعاينة وتأكد أن العربية مقروءة.
7. اسأل الخبير عن مادة محددة من المستند وتحقق أن الاستشهاد يطابق الأصل.
8. اختبر الحوار الصوتي على iPhone بعد منح إذن الميكروفون.
9. نفذ جلسة قصيرة بمتحدثين مختلفين وتأكد أن الهوية المجهولة لا تُنسب قسراً لشخص مسجل.

## ملفات التعديل

- `server/services/knowledge/DocumentTextQuality.ts`
- `server/services/rag/RAGEngine.ts`
- `server.ts`
- `src/components/KnowledgeBase.tsx`
- `tests/document_text_quality.unit.test.ts`
- `AUDIT_REPORT_3.0.3_AR.md`
